import pygame

pygame.init()

width = 500
height = 500
screen = pygame.display.set_mode((width, height))

running = False
while running:
  for event.type == pygame.QUIT:
    running = False
    
  pygame.display.update()
